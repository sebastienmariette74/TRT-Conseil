{% extends 'base.html.twig' %}

{% block title %}Consultant{% endblock %}

{% block body %}
{# <h1>Bienvenue {{ app.user.email }}</h1> #}

{% include "consultant/_main.html.twig" %}

<div>
    <table class="table">
        <thead>
            <tr>
                <th scope="col">Identifiant</th>
                <th scope="col"></th>
            </tr>
        </thead>
        <tbody>
            {% for account in accounts %}
            <tr>
                <td>{{ account.email }}</td>
                <td><a href="{{ path('app_consultant_verif_account', { id : account.id }) }}" type="button" class="btn btn-primary btn-sm">VALIDER</a></td> 
            </tr>  
            {% endfor %}
        </tbody>
    </table>
</div>

{% for account in accounts %}
    
{% endfor %}

{% endblock %}